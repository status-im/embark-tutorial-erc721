// /*global contract, config, it, assert*/
const SpaceshipToken = require('Embark/contracts/SpaceshipToken');

config({
  contracts: {
    "SpaceshipToken": {

    }
  }
});

contract("SpaceshipToken", function () {
  this.timeout(0);

  it("test1", async function () {
   
  });

});
